// Need back end for POST REQUESTS

// import {
//   CodeApi,
//   Language,
//   UpdateLatestCodeRequest,
//   CreateCodeRevisionRequest,
//   Code,
// } from '@codecharacter-2022/client';
// import { apiConfig, ApiError } from './ApiConfig';

/*
let languageInFormat = Language.Cpp;
if (language === 'c_cpp') languageInFormat = Language.Cpp;
else if (language === 'python') languageInFormat = Language.Python;

const bodyOfUpdateCodeRequest: UpdateLatestCodeRequest = {
  code: userCode,
  lock: false,
  language: languageInFormat,
};
codeAPI
  .updateLatestCode(bodyOfUpdateCodeRequest)
  .then()
  .catch(err => {
    if (err instanceof ApiError) console.log(err.message);
  });

*/
